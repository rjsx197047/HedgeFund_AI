// Shared site constants for the HedgeFund AI brochure.

/** The open-source GitHub repository. */
export const REPO_URL = 'https://github.com/rjsx197047/HedgeFund_AI';

/** Clone URL used in the Get Started steps. */
export const REPO_CLONE_URL = `${REPO_URL}.git`;

/** Issue tracker (used as the soft "contact" surface for an OSS project). */
export const ISSUES_URL = `${REPO_URL}/issues`;

/** Upstream project this fork builds on. */
export const UPSTREAM_URL = 'https://github.com/TauricResearch/TradingAgents';
